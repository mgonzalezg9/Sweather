import { Icon } from "@/interfaces";
import Colors from "./Colors";

const ICON_SIZE = 18;

const ICON_DEFAULT_PROPS: Icon = {
  size: ICON_SIZE,
  lightColor: Colors.palette.white,
  darkColor: Colors.palette.black,
};

export default ICON_DEFAULT_PROPS;